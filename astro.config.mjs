// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Utilizaremos fuentes de Google directamente en el Layout para mayor compatibilidad
});
